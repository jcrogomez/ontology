// GENERATED by `onto probe node_0020` (model: qwen3-coder:480b-cloud). Do not hand-edit;
// re-run probe to regenerate. 3 self-validated case(s)
// (each matched the source src/kernel/core/nodes/update-parent.ts under the behaviour-checker).
import type { BehaviorCase } from "../../src/laws/behavior-checker.js";

const __generatedCases: BehaviorCase[] = [
  {
    name: "rule:1 — node not found throws error",
    setup: () => ({ id: "nonexistent", newParentNodeId: "parent1" }),
    invoke: (api, ctx) => {
      try {
        (api as any).updateNodeParent(ctx);
        return false;
      } catch (e) {
        return (e as Error).message.includes("Node not found");
      }
    },
    assert: (r) => r === true,
  },
  {
    name: "rule:1 — new parent not found throws error",
    setup: () => ({ id: "node1", newParentNodeId: "nonexistent" }),
    invoke: (api, ctx) => {
      try {
        (api as any).updateNodeParent(ctx);
        return false;
      } catch (e) {
        return (e as Error).message.includes("New parent node not found");
      }
    },
    assert: (r) => r === true,
  },
  {
    name: "rule:2 — cross-branch reparenting refused",
    setup: () => ({ id: "node1", newParentNodeId: "parent1" }),
    invoke: (api, ctx) => {
      try {
        (api as any).updateNodeParent(ctx);
        return false;
      } catch (e) {
        return (e as Error).message.includes("Cross-branch reparenting refused");
      }
    },
    assert: (r) => r === true,
  },
  {
    name: "rule:3 — cycle detection prevents reparenting",
    setup: () => ({ id: "child1", newParentNodeId: "descendant1" }),
    invoke: (api, ctx) => {
      try {
        (api as any).updateNodeParent(ctx);
        return false;
      } catch (e) {
        return (e as Error).message.includes("would create a cycle");
      }
    },
    assert: (r) => r === true,
  },
  {
    name: "wouldCreateCycle returns true for direct cycle",
    setup: () => ({ nodeId: "nodeA", newParentId: "nodeA" }),
    invoke: (api, ctx) => (api as any).wouldCreateCycle(ctx.nodeId, ctx.newParentId),
    assert: (r) => r === true,
  },
  {
    name: "wouldCreateCycle returns false for valid parent",
    setup: () => ({ nodeId: "nodeA", newParentId: "validParent" }),
    invoke: (api, ctx) => (api as any).wouldCreateCycle(ctx.nodeId, ctx.newParentId),
    assert: (r) => r === false,
  },
  {
    name: "wouldCreateCycle detects indirect cycle",
    setup: () => ({ nodeId: "parent", newParentId: "child" }),
    invoke: (api, ctx) => (api as any).wouldCreateCycle(ctx.nodeId, ctx.newParentId),
    assert: (r) => r === true,
  },
  {
    name: "identity reparenting throws error",
    setup: () => ({ id: "node1", newParentNodeId: "sameParent" }),
    invoke: (api, ctx) => {
      try {
        (api as any).updateNodeParent(ctx);
        return false;
      } catch (e) {
        return (e as Error).message.includes("already a child of");
      }
    },
    assert: (r) => r === true,
  },
];
// Only the cases that self-validated against the source are exported.
export const cases = __generatedCases.filter((_, i) => new Set([0, 4, 5]).has(i));
