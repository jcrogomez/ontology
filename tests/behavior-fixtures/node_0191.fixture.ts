// GENERATED by `onto probe node_0191` (model: qwen3-coder:480b-cloud). Do not hand-edit;
// re-run probe to regenerate. 6 self-validated case(s)
// (each matched the source src/surfaces/walker/actions/verify-from-walker.ts under the behaviour-checker).
import type { BehaviorCase } from "../../src/laws/behavior-checker.js";

const __generatedCases: BehaviorCase[] = [
  {
    name: "rule:1 — returns error when node has no outputs.files[0]",
    setup: () => ({
      focal: {
        id: "test-node",
        outputs: {},
      },
    }),
    invoke: (api, ctx) => (api as any).verifyFromWalker(ctx.focal as any),
    assert: (r) => r.ok === false && r.message.includes("nothing to verify"),
  },
  {
    name: "rule:2 — returns error when source file does not exist",
    setup: () => ({
      focal: {
        id: "test-node",
        outputs: {
          files: ["nonexistent.ts"],
        },
      },
    }),
    invoke: (api, ctx) => (api as any).verifyFromWalker(ctx.focal as any, "/fake/cwd"),
    assert: (r) => r.ok === false && r.message.includes("source file not found"),
  },
  {
    name: "rule:3 — returns error when no compiled artifact exists",
    setup: () => ({
      focal: {
        id: "test-node",
        outputs: {
          files: ["source.ts"],
        },
      },
    }),
    invoke: (api, ctx) => (api as any).verifyFromWalker(ctx.focal as any, "/fake/cwd"),
    assert: (r) => r.ok === false && r.message.includes("no compiled artifact"),
  },
  {
    name: "rule:4 — returns valid result with correct structure when successful",
    setup: () => ({
      focal: {
        id: "test-node",
        outputs: {
          files: ["source.ts"],
        },
        technical: {
          language: "typescript",
        },
      },
    }),
    invoke: (api, ctx) => (api as any).verifyFromWalker(ctx.focal as any, "/fake/cwd"),
    assert: (r) => r.ok === true && typeof r.verdict === "string" && typeof r.metrics === "object",
  },
  {
    name: "languageHint returns typescript for ts language tag",
    setup: () => ({
      node: {
        technical: { language: "ts" },
        outputs: { files: ["test.py"] },
      },
    }),
    invoke: (api, ctx) => (api as any).languageHint(ctx.node),
    assert: (r) => r === "typescript",
  },
  {
    name: "languageHint returns python for py language tag",
    setup: () => ({
      node: {
        technical: { language: "py" },
        outputs: { files: ["test.ts"] },
      },
    }),
    invoke: (api, ctx) => (api as any).languageHint(ctx.node),
    assert: (r) => r === "python",
  },
  {
    name: "languageHint falls back to file extension when no language tag",
    setup: () => ({
      node: {
        technical: {},
        outputs: { files: ["test.py"] },
      },
    }),
    invoke: (api, ctx) => (api as any).languageHint(ctx.node),
    assert: (r) => r === "python",
  },
  {
    name: "languageHint returns unknown when no language info available",
    setup: () => ({
      node: {
        technical: {},
        outputs: { files: ["test.unknown"] },
      },
    }),
    invoke: (api, ctx) => (api as any).languageHint(ctx.node),
    assert: (r) => r === "unknown",
  },
];
// Only the cases that self-validated against the source are exported.
export const cases = __generatedCases.filter((_, i) => new Set([0, 1, 4, 5, 6, 7]).has(i));
