// GENERATED by `onto probe node_0219` (model: qwen3-coder:480b-cloud). Do not hand-edit;
// re-run probe to regenerate. 7 self-validated case(s)
// (each matched the source src/surfaces/walker/theme/colors.ts under the behaviour-checker).
import type { BehaviorCase } from "../../src/laws/behavior-checker.js";

const __generatedCases: BehaviorCase[] = [
  {
    name: "rule:1 — POSET_COLORS maps each AbstractionLevel to exact string color",
    setup: () => ({}),
    invoke: (api) => api.POSET_COLORS,
    assert: (r) => 
      r.canon === "white" &&
      r.project === "blue" &&
      r.target === "cyan" &&
      r.stack === "green" &&
      r.architecture === "greenBright" &&
      r.domain === "yellow" &&
      r.workflow === "yellowBright" &&
      r.interface === "magenta" &&
      r.unit === "red" &&
      r.token === "gray" &&
      r.artifact === "whiteBright"
  },
  {
    name: "rule:2 — colorsEnabled returns true when NO_COLOR is undefined and stdout is TTY",
    setup: () => {
      const originalEnv = process.env.NO_COLOR;
      const originalStdout = process.stdout;
      process.env.NO_COLOR = undefined;
      Object.defineProperty(process, 'stdout', { value: { isTTY: true }, writable: true });
      return { originalEnv, originalStdout };
    },
    invoke: (api, ctx) => {
      const result = api.colorsEnabled();
      process.env.NO_COLOR = ctx.originalEnv;
      process.stdout = ctx.originalStdout;
      return result;
    },
    assert: (r) => r === true
  },
  {
    name: "rule:2 — colorsEnabled returns false when NO_COLOR is set to non-empty string",
    setup: () => {
      const originalEnv = process.env.NO_COLOR;
      process.env.NO_COLOR = "1";
      return { originalEnv };
    },
    invoke: (api, ctx) => {
      const result = api.colorsEnabled();
      process.env.NO_COLOR = ctx.originalEnv;
      return result;
    },
    assert: (r) => r === false
  },
  {
    name: "rule:2 — colorsEnabled returns false when stdout is not TTY and NO_COLOR is empty",
    setup: () => {
      const originalEnv = process.env.NO_COLOR;
      const originalStdout = process.stdout;
      process.env.NO_COLOR = "";
      Object.defineProperty(process, 'stdout', { value: { isTTY: false }, writable: true });
      return { originalEnv, originalStdout };
    },
    invoke: (api, ctx) => {
      const result = api.colorsEnabled();
      process.env.NO_COLOR = ctx.originalEnv;
      process.stdout = ctx.originalStdout;
      return result;
    },
    assert: (r) => r === false
  },
  {
    name: "rule:3 — levelTag returns uppercase level in brackets",
    setup: () => ({ level: "domain" as const }),
    invoke: (api, ctx) => api.levelTag(ctx.level),
    assert: (r) => r === "[DOMAIN]"
  },
  {
    name: "levelTag works for all AbstractionLevel values",
    setup: () => ({ level: "canon" as const }),
    invoke: (api, ctx) => api.levelTag(ctx.level),
    assert: (r) => r === "[CANON]"
  },
  {
    name: "levelTag with token level",
    setup: () => ({ level: "token" as const }),
    invoke: (api, ctx) => api.levelTag(ctx.level),
    assert: (r) => r === "[TOKEN]"
  },
  {
    name: "levelTag with artifact level",
    setup: () => ({ level: "artifact" as const }),
    invoke: (api, ctx) => api.levelTag(ctx.level),
    assert: (r) => r === "[ARTIFACT]"
  }
];
// Only the cases that self-validated against the source are exported.
export const cases = __generatedCases.filter((_, i) => new Set([0, 2, 3, 4, 5, 6, 7]).has(i));
