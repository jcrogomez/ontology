// GENERATED by `onto probe node_0130` (model: qwen3-coder:480b-cloud). Do not hand-edit;
// re-run probe to regenerate. 7 self-validated case(s)
// (each matched the source src/laws/failure-mode-tagger.ts under the behaviour-checker).
import type { BehaviorCase } from "../../src/laws/behavior-checker.js";
import type {
  FailureMode,
  FailureModeTaggerInput,
  CompileBackExportIntegration,
} from "../../src/laws/failure-mode-tagger.js";

const __generatedCases: BehaviorCase[] = [
  {
    name: "rule:1 — tagFailureModes returns stable ordered modes",
    setup: () => ({
      input: {
        ok: false,
        failure: "Gluing failed due to missing requirement",
        recovery: {
          astExportsCount: 3,
          regeneratedExportsCount: 2,
          missingMandatoryExports: ["foo"],
          hallucinatedExports: ["bar"],
        } satisfies CompileBackExportIntegration,
      },
    }),
    invoke: (api, ctx) => api.tagFailureModes(ctx.input),
    assert: (r) => {
      const expected: FailureMode[] = ["compile_back_failed"];
      return JSON.stringify(r) === JSON.stringify(expected);
    },
  },
  {
    name: "rule:2 — tagFailureModes handles empty regen",
    setup: () => ({
      input: {
        ok: true,
        failure: undefined,
        recovery: {
          astExportsCount: 3,
          regeneratedExportsCount: 0,
          missingMandatoryExports: [],
          hallucinatedExports: [],
        } satisfies CompileBackExportIntegration,
      },
    }),
    invoke: (api, ctx) => api.tagFailureModes(ctx.input),
    assert: (r) => {
      const expected: FailureMode[] = ["empty_regen"];
      return JSON.stringify(r) === JSON.stringify(expected);
    },
  },
  {
    name: "rule:3 — tagFailureModes detects missing exports",
    setup: () => ({
      input: {
        ok: true,
        failure: undefined,
        recovery: {
          astExportsCount: 3,
          regeneratedExportsCount: 2,
          missingMandatoryExports: ["foo"],
          hallucinatedExports: [],
        } satisfies CompileBackExportIntegration,
      },
    }),
    invoke: (api, ctx) => api.tagFailureModes(ctx.input),
    assert: (r) => {
      const expected: FailureMode[] = ["missing_exports"];
      return JSON.stringify(r) === JSON.stringify(expected);
    },
  },
  {
    name: "rule:4 — tagFailureModes detects hallucinated exports",
    setup: () => ({
      input: {
        ok: true,
        failure: undefined,
        recovery: {
          astExportsCount: 3,
          regeneratedExportsCount: 2,
          missingMandatoryExports: [],
          hallucinatedExports: ["bar"],
        } satisfies CompileBackExportIntegration,
      },
    }),
    invoke: (api, ctx) => api.tagFailureModes(ctx.input),
    assert: (r) => {
      const expected: FailureMode[] = ["hallucinated_exports"];
      return JSON.stringify(r) === JSON.stringify(expected);
    },
  },
  {
    name: "rule:5 — tagFailureModes detects gluing rejection",
    setup: () => ({
      input: {
        ok: true,
        failure: "Gluing failed due to missing requirement",
        recovery: {
          astExportsCount: 0,
          regeneratedExportsCount: 0,
          missingMandatoryExports: [],
          hallucinatedExports: [],
        } satisfies CompileBackExportIntegration,
      },
    }),
    invoke: (api, ctx) => api.tagFailureModes(ctx.input),
    assert: (r) => {
      const expected: FailureMode[] = ["gluing_rejected"];
      return JSON.stringify(r) === JSON.stringify(expected);
    },
  },
  {
    name: "rule:6 — tagFailureModes detects schema invalidation",
    setup: () => ({
      input: {
        ok: true,
        failure: "Schema validation failed with Zod error",
        recovery: {
          astExportsCount: 0,
          regeneratedExportsCount: 0,
          missingMandatoryExports: [],
          hallucinatedExports: [],
        } satisfies CompileBackExportIntegration,
      },
    }),
    invoke: (api, ctx) => api.tagFailureModes(ctx.input),
    assert: (r) => {
      const expected: FailureMode[] = ["schema_invalid"];
      return JSON.stringify(r) === JSON.stringify(expected);
    },
  },
  {
    name: "rule:7 — aggregateFailureModes produces correct aggregate",
    setup: () => ({
      reports: [
        { nodeId: "node1", modes: ["missing_exports"] },
        { nodeId: "node2", modes: ["hallucinated_exports", "schema_invalid"] },
        { nodeId: "node3", modes: [] },
      ],
    }),
    invoke: (api, ctx) => api.aggregateFailureModes(ctx.reports),
    assert: (r) => {
      return (
        r.affectedNodes === 2 &&
        r.totalInspected === 3 &&
        r.counts.missing_exports === 1 &&
        r.counts.hallucinated_exports === 1 &&
        r.counts.schema_invalid === 1 &&
        r.perNode.length === 3
      );
    },
  },
  {
    name: "rule:8 — aggregateFailureModes handles empty input",
    setup: () => ({
      reports: [],
    }),
    invoke: (api, ctx) => api.aggregateFailureModes(ctx.reports),
    assert: (r) => {
      return (
        r.affectedNodes === 0 &&
        r.totalInspected === 0 &&
        Object.values(r.counts).every((c) => c === 0) &&
        r.perNode.length === 0
      );
    },
  },
];
// Only the cases that self-validated against the source are exported.
export const cases = __generatedCases.filter((_, i) => new Set([1, 2, 3, 4, 5, 6, 7]).has(i));
