// GENERATED by `onto probe node_0104` (model: qwen3-coder:480b-cloud). Do not hand-edit;
// re-run probe to regenerate. 8 self-validated case(s)
// (each matched the source src/forward/context/presheaf.ts under the behaviour-checker).
import type { BehaviorCase } from "../../src/laws/behavior-checker.js";

const __generatedCases: BehaviorCase[] = [
  {
    name: "rule:1 — buildFragment maps node context provides to fragment provides",
    setup: () => ({
      node: {
        id: "node1",
        coordinates: { branch: "main" },
        context: {
          provides: [{ key: "key1" }, { key: "key2" }],
          requires: [],
          forbids: [],
          optional: [],
        },
        rules: [],
      },
    }),
    invoke: (api, ctx) => api.buildFragment(ctx.node),
    assert: (r) => r.provides.length === 2 && r.provides[0] === "key1" && r.provides[1] === "key2",
  },
  {
    name: "rule:2 — buildFragment includes provideSignatures when signatures are present",
    setup: () => ({
      node: {
        id: "node1",
        coordinates: { branch: "main" },
        context: {
          provides: [{ key: "key1", signature: "sig1" }, { key: "key2" }],
          requires: [],
          forbids: [],
          optional: [],
        },
        rules: [],
      },
    }),
    invoke: (api, ctx) => api.buildFragment(ctx.node),
    assert: (r) => 
      r.provideSignatures !== undefined && 
      r.provideSignatures["key1"] === "sig1" && 
      r.provideSignatures["key2"] === undefined,
  },
  {
    name: "rule:3 — buildFragment omits provideSignatures when no signatures are present",
    setup: () => ({
      node: {
        id: "node1",
        coordinates: { branch: "main" },
        context: {
          provides: [{ key: "key1" }, { key: "key2" }],
          requires: [],
          forbids: [],
          optional: [],
        },
        rules: [],
      },
    }),
    invoke: (api, ctx) => api.buildFragment(ctx.node),
    assert: (r) => r.provideSignatures === undefined,
  },
  {
    name: "rule:4 — buildFragment maps requires, forbids, and optional sources correctly",
    setup: () => ({
      node: {
        id: "node1",
        coordinates: { branch: "main" },
        context: {
          provides: [],
          requires: [{ source: "req1" }, { source: "req2" }],
          forbids: [{ source: "forbid1" }],
          optional: [{ source: "opt1" }],
        },
        rules: [],
      },
    }),
    invoke: (api, ctx) => api.buildFragment(ctx.node),
    assert: (r) => 
      r.requires.length === 2 && 
      r.requires[0] === "req1" && 
      r.forbids[0] === "forbid1" && 
      r.optional[0] === "opt1",
  },
  {
    name: "rule:5 — buildFragment strips rule numbering prefixes",
    setup: () => ({
      node: {
        id: "node1",
        coordinates: { branch: "main" },
        context: {
          provides: [],
          requires: [],
          forbids: [],
          optional: [],
        },
        rules: ["1. Rule One", "2. Rule Two", "Rule Three"],
      },
    }),
    invoke: (api, ctx) => api.buildFragment(ctx.node),
    assert: (r) => 
      r.rules.length === 3 && 
      r.rules[0] === "Rule One" && 
      r.rules[1] === "Rule Two" && 
      r.rules[2] === "Rule Three",
  },
  {
    name: "rule:6 — buildFragment handles empty context arrays",
    setup: () => ({
      node: {
        id: "node1",
        coordinates: { branch: "main" },
        context: {
          provides: [],
          requires: [],
          forbids: [],
          optional: [],
        },
        rules: [],
      },
    }),
    invoke: (api, ctx) => api.buildFragment(ctx.node),
    assert: (r) => 
      r.provides.length === 0 && 
      r.requires.length === 0 && 
      r.forbids.length === 0 && 
      r.optional.length === 0 && 
      r.rules.length === 0,
  },
  {
    name: "rule:7 — buildFragment sets nodeId and branch from node",
    setup: () => ({
      node: {
        id: "node123",
        coordinates: { branch: "feature-branch" },
        context: {
          provides: [],
          requires: [],
          forbids: [],
          optional: [],
        },
        rules: [],
      },
    }),
    invoke: (api, ctx) => api.buildFragment(ctx.node),
    assert: (r) => r.nodeId === "node123" && r.branch === "feature-branch",
  },
  {
    name: "rule:8 — buildFragment handles mixed signed and unsigned provides",
    setup: () => ({
      node: {
        id: "node1",
        coordinates: { branch: "main" },
        context: {
          provides: [
            { key: "key1", signature: "sig1" },
            { key: "key2" },
            { key: "key3", signature: "sig3" },
          ],
          requires: [],
          forbids: [],
          optional: [],
        },
        rules: [],
      },
    }),
    invoke: (api, ctx) => api.buildFragment(ctx.node),
    assert: (r) => 
      r.provideSignatures !== undefined && 
      r.provideSignatures["key1"] === "sig1" && 
      r.provideSignatures["key2"] === undefined && 
      r.provideSignatures["key3"] === "sig3",
  },
];
// Only the cases that self-validated against the source are exported.
export const cases = __generatedCases.filter((_, i) => new Set([0, 1, 2, 3, 4, 5, 6, 7]).has(i));
