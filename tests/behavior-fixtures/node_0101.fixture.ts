// GENERATED by `onto probe node_0101` (model: qwen3-coder:480b-cloud). Do not hand-edit;
// re-run probe to regenerate. 7 self-validated case(s)
// (each matched the source src/forward/context/edge-suggester.ts under the behaviour-checker).
import type { BehaviorCase } from "../../src/laws/behavior-checker.js";

const __generatedCases: BehaviorCase[] = [
  {
    name: "rule:1 — returns empty array when missingRequirements is empty",
    setup: () => ({
      focalNode: {
        id: "node1",
        coordinates: { branch: "main", abstraction: "low" },
        context: { requires: [], provides: [] },
      },
      missingRequirements: [],
      allNodes: [],
      existingEdges: [],
    }),
    invoke: (api, ctx) => api.suggestEdgeProposals(ctx),
    assert: (r) => Array.isArray(r) && r.length === 0,
  },
  {
    name: "rule:2 — skips focal node as provider",
    setup: () => ({
      focalNode: {
        id: "node1",
        coordinates: { branch: "main", abstraction: "low" },
        context: { requires: [], provides: [{ key: "tokenA" }] },
      },
      missingRequirements: ["tokenA"],
      allNodes: [
        {
          id: "node1",
          coordinates: { branch: "main", abstraction: "low" },
          context: { requires: [], provides: [{ key: "tokenA" }] },
        },
      ],
      existingEdges: [],
    }),
    invoke: (api, ctx) => api.suggestEdgeProposals(ctx),
    assert: (r) => Array.isArray(r) && r.length === 0,
  },
  {
    name: "rule:3 — respects branch constraint",
    setup: () => ({
      focalNode: {
        id: "node1",
        coordinates: { branch: "main", abstraction: "low" },
        context: { requires: [], provides: [] },
      },
      missingRequirements: ["tokenA"],
      allNodes: [
        {
          id: "node2",
          coordinates: { branch: "dev", abstraction: "low" },
          context: { requires: [], provides: [{ key: "tokenA" }] },
        },
      ],
      existingEdges: [],
    }),
    invoke: (api, ctx) => api.suggestEdgeProposals(ctx),
    assert: (r) => Array.isArray(r) && r.length === 0,
  },
  {
    name: "rule:4 — skips existing edges",
    setup: () => ({
      focalNode: {
        id: "node1",
        coordinates: { branch: "main", abstraction: "low" },
        context: { requires: [], provides: [] },
      },
      missingRequirements: ["tokenA"],
      allNodes: [
        {
          id: "node2",
          coordinates: { branch: "main", abstraction: "low" },
          context: { requires: [], provides: [{ key: "tokenA" }] },
        },
      ],
      existingEdges: [
        {
          from: "node1",
          to: "node2",
          type: "depends_on",
        } as any,
      ],
    }),
    invoke: (api, ctx) => api.suggestEdgeProposals(ctx),
    assert: (r) =>
      Array.isArray(r) &&
      r.length === 1 &&
      r[0].type === "uses_token" &&
      r[0].to === "node2",
  },
  {
    name: "rule:5 — applies maxProvidersPerToken cap",
    setup: () => {
      const nodes = [
        {
          id: "node1",
          coordinates: { branch: "main", abstraction: "low" },
          context: { requires: [], provides: [] },
        },
      ];
      for (let i = 2; i <= 10; i++) {
        nodes.push({
          id: `node${i}`,
          coordinates: { branch: "main", abstraction: "low" },
          context: { requires: [], provides: [{ key: "tokenA" }] },
        });
      }
      return {
        focalNode: nodes[0],
        missingRequirements: ["tokenA"],
        allNodes: nodes,
        existingEdges: [],
        maxProvidersPerToken: 3,
      };
    },
    invoke: (api, ctx) => api.suggestEdgeProposals(ctx),
    assert: (r) =>
      Array.isArray(r) &&
      r.length === 6 && // 3 providers * 2 edge types
      new Set(r.map((s) => s.to)).size === 3,
  },
  {
    name: "rule:6 — deterministic sorting by (to, type)",
    setup: () => ({
      focalNode: {
        id: "node1",
        coordinates: { branch: "main", abstraction: "low" },
        context: { requires: [], provides: [] },
      },
      missingRequirements: ["tokenA"],
      allNodes: [
        {
          id: "node3",
          coordinates: { branch: "main", abstraction: "low" },
          context: { requires: [], provides: [{ key: "tokenA" }] },
        },
        {
          id: "node2",
          coordinates: { branch: "main", abstraction: "low" },
          context: { requires: [], provides: [{ key: "tokenA" }] },
        },
      ],
      existingEdges: [],
    }),
    invoke: (api, ctx) => api.suggestEdgeProposals(ctx),
    assert: (r) =>
      Array.isArray(r) &&
      r.length === 4 &&
      r[0].to === "node2" &&
      r[0].type === "depends_on" &&
      r[1].to === "node2" &&
      r[1].type === "uses_token" &&
      r[2].to === "node3" &&
      r[2].type === "depends_on" &&
      r[3].to === "node3" &&
      r[3].type === "uses_token",
  },
  {
    name: "rule:7 — validates edge direction",
    setup: () => ({
      focalNode: {
        id: "node1",
        coordinates: { branch: "main", abstraction: "high" },
        context: { requires: [], provides: [] },
      },
      missingRequirements: ["tokenA"],
      allNodes: [
        {
          id: "node2",
          coordinates: { branch: "main", abstraction: "low" },
          context: { requires: [], provides: [{ key: "tokenA" }] },
        },
      ],
      existingEdges: [],
      edgeTypes: ["depends_on"],
    }),
    invoke: (api, ctx) => api.suggestEdgeProposals(ctx),
    assert: (r) => Array.isArray(r) && r.length === 0,
  },
  {
    name: "typical case with multiple tokens and providers",
    setup: () => ({
      focalNode: {
        id: "focal",
        coordinates: { branch: "main", abstraction: "mid" },
        context: { requires: [], provides: [] },
      },
      missingRequirements: ["tokenA", "tokenB"],
      allNodes: [
        {
          id: "focal",
          coordinates: { branch: "main", abstraction: "mid" },
          context: { requires: [], provides: [] },
        },
        {
          id: "providerA",
          coordinates: { branch: "main", abstraction: "low" },
          context: { requires: [], provides: [{ key: "tokenA" }] },
        },
        {
          id: "providerB",
          coordinates: { branch: "main", abstraction: "low" },
          context: { requires: [], provides: [{ key: "tokenB" }] },
        },
        {
          id: "providerAB",
          coordinates: { branch: "main", abstraction: "low" },
          context: { requires: [], provides: [{ key: "tokenA" }, { key: "tokenB" }] },
        },
      ],
      existingEdges: [],
    }),
    invoke: (api, ctx) => api.suggestEdgeProposals(ctx),
    assert: (r) =>
      Array.isArray(r) &&
      r.length === 6 && // 3 providers * 2 edge types
      r.every(
        (s) =>
          s.from === "focal" &&
          ["providerA", "providerB", "providerAB"].includes(s.to) &&
          ["depends_on", "uses_token"].includes(s.type)
      ) &&
      r.find((s) => s.to === "providerA")!.satisfies.length === 1 &&
      r.find((s) => s.to === "providerB")!.satisfies.length === 1 &&
      r.find((s) => s.to === "providerAB")!.satisfies.length === 2,
  },
];
// Only the cases that self-validated against the source are exported.
export const cases = __generatedCases.filter((_, i) => new Set([0, 1, 2, 3, 4, 5, 7]).has(i));
