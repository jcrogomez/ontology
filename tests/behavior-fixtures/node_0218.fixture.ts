// GENERATED by `onto probe node_0218` (model: qwen3-coder:480b-cloud). Do not hand-edit;
// re-run probe to regenerate. 8 self-validated case(s)
// (each matched the source src/surfaces/walker/state/shared-tokens.ts under the behaviour-checker).
import type { BehaviorCase } from "../../src/laws/behavior-checker.js";

const __generatedCases: BehaviorCase[] = [
  {
    name: "rule:1 — focalRequiresShared returns one entry per requires.source on the focal node",
    setup: () => ({
      neighborhood: {
        focal: {
          id: "focal1",
          context: {
            requires: [{ source: "tokenA" }, { source: "tokenB" }],
            provides: [{ key: "tokenC" }],
          },
        },
        pathToCanon: [],
        siblings: [],
        children: [],
        edgeNeighbors: [],
      },
    }),
    invoke: (api, ctx) => api.focalRequiresShared(ctx.neighborhood),
    assert: (r) => Array.isArray(r) && r.length === 2,
  },
  {
    name: "rule:2 — focalProvidesShared returns one entry per provides.key on the focal node",
    setup: () => ({
      neighborhood: {
        focal: {
          id: "focal1",
          context: {
            requires: [{ source: "tokenA" }],
            provides: [{ key: "tokenB" }, { key: "tokenC" }, { key: "tokenD" }],
          },
        },
        pathToCanon: [],
        siblings: [],
        children: [],
        edgeNeighbors: [],
      },
    }),
    invoke: (api, ctx) => api.focalProvidesShared(ctx.neighborhood),
    assert: (r) => Array.isArray(r) && r.length === 3,
  },
  {
    name: "sharedWith includes node IDs that mention the same token in requires",
    setup: () => ({
      neighborhood: {
        focal: {
          id: "focal1",
          context: {
            requires: [{ source: "sharedToken" }],
            provides: [],
          },
        },
        pathToCanon: [],
        siblings: [
          {
            id: "sibling1",
            context: {
              requires: [{ source: "sharedToken" }],
              provides: [],
            },
          },
        ],
        children: [],
        edgeNeighbors: [],
      },
    }),
    invoke: (api, ctx) => api.focalRequiresShared(ctx.neighborhood),
    assert: (r) => r[0].sharedWith.includes("sibling1"),
  },
  {
    name: "sharedWith includes node IDs that mention the same token in provides",
    setup: () => ({
      neighborhood: {
        focal: {
          id: "focal1",
          context: {
            requires: [],
            provides: [{ key: "sharedToken" }],
          },
        },
        pathToCanon: [],
        siblings: [],
        children: [],
        edgeNeighbors: [
          {
            id: "neighbor1",
            context: {
              requires: [],
              provides: [{ key: "sharedToken" }],
            },
          },
        ],
      },
    }),
    invoke: (api, ctx) => api.focalProvidesShared(ctx.neighborhood),
    assert: (r) => r[0].sharedWith.includes("neighbor1"),
  },
  {
    name: "sharedWith is sorted alphabetically by node ID",
    setup: () => ({
      neighborhood: {
        focal: {
          id: "focal1",
          context: {
            requires: [{ source: "token" }],
            provides: [],
          },
        },
        pathToCanon: [],
        siblings: [],
        children: [
          { id: "zNode", context: { requires: [{ source: "token" }], provides: [] } },
          { id: "aNode", context: { requires: [{ source: "token" }], provides: [] } },
        ],
        edgeNeighbors: [],
      },
    }),
    invoke: (api, ctx) => api.focalRequiresShared(ctx.neighborhood),
    assert: (r) => {
      const sharedWith = r[0].sharedWith;
      return sharedWith[0] === "aNode" && sharedWith[1] === "zNode";
    },
  },
  {
    name: "focal node is excluded from sharedWith even if it contains the token",
    setup: () => ({
      neighborhood: {
        focal: {
          id: "focal1",
          context: {
            requires: [{ source: "selfToken" }],
            provides: [{ key: "selfToken" }],
          },
        },
        pathToCanon: [],
        siblings: [],
        children: [],
        edgeNeighbors: [],
      },
    }),
    invoke: (api, ctx) => api.focalRequiresShared(ctx.neighborhood),
    assert: (r) => r[0].sharedWith.length === 0,
  },
  {
    name: "duplicate nodes in neighborhood candidates are deduplicated",
    setup: () => ({
      neighborhood: {
        focal: {
          id: "focal1",
          context: {
            requires: [{ source: "dupToken" }],
            provides: [],
          },
        },
        pathToCanon: [
          {
            id: "dupNode",
            context: {
              requires: [{ source: "dupToken" }],
              provides: [],
            },
          },
        ],
        siblings: [
          {
            id: "dupNode", // Same ID as above
            context: {
              requires: [{ source: "otherToken" }],
              provides: [],
            },
          },
        ],
        children: [],
        edgeNeighbors: [],
      },
    }),
    invoke: (api, ctx) => api.focalRequiresShared(ctx.neighborhood),
    assert: (r) => r[0].sharedWith.length === 1,
  },
  {
    name: "empty neighborhood returns empty sharedWith arrays",
    setup: () => ({
      neighborhood: {
        focal: {
          id: "focal1",
          context: {
            requires: [{ source: "token" }],
            provides: [],
          },
        },
        pathToCanon: [],
        siblings: [],
        children: [],
        edgeNeighbors: [],
      },
    }),
    invoke: (api, ctx) => api.focalRequiresShared(ctx.neighborhood),
    assert: (r) => r[0].sharedWith.length === 0,
  },
];
// Only the cases that self-validated against the source are exported.
export const cases = __generatedCases.filter((_, i) => new Set([0, 1, 2, 3, 4, 5, 6, 7]).has(i));
