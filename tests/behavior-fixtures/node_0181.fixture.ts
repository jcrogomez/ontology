// GENERATED by `onto probe node_0181` (model: qwen3-coder:480b-cloud). Do not hand-edit;
// re-run probe to regenerate. 6 self-validated case(s)
// (each matched the source src/surfaces/walker/actions/context-from-walker.ts under the behaviour-checker).
import type { BehaviorCase } from "../../src/laws/behavior-checker.js";

const __generatedCases: BehaviorCase[] = [
  {
    name: "rule:1 — returns ok true with output when assembly succeeds",
    setup: () => ({ focalId: "test-node-id", cwd: "/test/path" }),
    invoke: (api, ctx) => (api as any).contextFromWalker(ctx.focalId, ctx.cwd),
    assert: (r) => r.ok === true && r.output !== undefined && r.message === undefined,
  },
  {
    name: "rule:2 — returns ok false with message when assembly throws",
    setup: () => ({ focalId: "invalid-node-id", cwd: "/invalid/path" }),
    invoke: (api, ctx) => (api as any).contextFromWalker(ctx.focalId, ctx.cwd),
    assert: (r) => r.ok === false && r.output === undefined && typeof r.message === "string",
  },
  {
    name: "rule:3 — handles undefined cwd parameter",
    setup: () => ({ focalId: "test-node-id" }),
    invoke: (api, ctx) => (api as any).contextFromWalker(ctx.focalId),
    assert: (r) => r.ok === true && r.output !== undefined && r.message === undefined,
  },
  {
    name: "rule:4 — returns structured result with proper shape",
    setup: () => ({ focalId: "test-node-id" }),
    invoke: (api, ctx) => (api as any).contextFromWalker(ctx.focalId),
    assert: (r) => typeof r === "object" && "ok" in r && typeof r.ok === "boolean",
  },
  {
    name: "typical case with valid node and path",
    setup: () => ({ focalId: "node-123", cwd: "/home/user/project" }),
    invoke: (api, ctx) => (api as any).contextFromWalker(ctx.focalId, ctx.cwd),
    assert: (r) => typeof r === "object" && "ok" in r,
  },
  {
    name: "empty string focalId",
    setup: () => ({ focalId: "", cwd: "/test" }),
    invoke: (api, ctx) => (api as any).contextFromWalker(ctx.focalId, ctx.cwd),
    assert: (r) => typeof r === "object" && "ok" in r,
  },
  {
    name: "both parameters provided",
    setup: () => ({ focalId: "node-456", cwd: "/another/path" }),
    invoke: (api, ctx) => (api as any).contextFromWalker(ctx.focalId, ctx.cwd),
    assert: (r) => typeof r === "object" && "ok" in r,
  },
  {
    name: "minimal valid call",
    setup: () => ({ focalId: "minimal-node" }),
    invoke: (api, ctx) => (api as any).contextFromWalker(ctx.focalId),
    assert: (r) => typeof r === "object" && "ok" in r,
  },
];
// Only the cases that self-validated against the source are exported.
export const cases = __generatedCases.filter((_, i) => new Set([1, 3, 4, 5, 6, 7]).has(i));
