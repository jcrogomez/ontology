// GENERATED by `onto probe node_0140` (model: qwen3-coder:480b-cloud). Do not hand-edit;
// re-run probe to regenerate. 7 self-validated case(s)
// (each matched the source src/inverse/structural-classifier.ts under the behaviour-checker).
import type { BehaviorCase } from "../../src/laws/behavior-checker.js";

const __generatedCases: BehaviorCase[] = [
  {
    name: "rule:1 — test file detection by path suffix",
    setup: () => ({ path: "/src/foo.test.ts", content: "describe('foo', () => {})" }),
    invoke: (api, ctx) => api.classifySourceFile(ctx),
    assert: (r) => r.structuralShape === "test_module" && r.semanticRole === "test_specification",
  },
  {
    name: "rule:2 — configuration file by basename",
    setup: () => ({ path: "/project/vite.config.ts", content: "export default {}" }),
    invoke: (api, ctx) => api.classifySourceFile(ctx),
    assert: (r) => r.structuralShape === "configuration_module" && r.semanticRole === "configuration",
  },
  {
    name: "rule:3 — barrel module with only re-exports",
    setup: () => ({
      path: "/src/index.ts",
      content: "export { foo } from './foo'; export * from './bar';",
    }),
    invoke: (api, ctx) => api.classifySourceFile(ctx),
    assert: (r) => r.structuralShape === "barrel" && r.semanticRole === "module_boundary",
  },
  {
    name: "rule:4 — declaration-only module with interfaces",
    setup: () => ({
      path: "/src/types.ts",
      content: "export interface User { id: string; } export type Role = 'admin' | 'user';",
    }),
    invoke: (api, ctx) => api.classifySourceFile(ctx),
    assert: (r) => r.structuralShape === "declaration_only" && r.semanticRole === "utility",
  },
  {
    name: "rule:5 — schema module with Zod",
    setup: () => ({
      path: "/src/schema.ts",
      content: "import { z } from 'zod'; export const userSchema = z.object({ id: z.string() });",
    }),
    invoke: (api, ctx) => api.classifySourceFile(ctx),
    assert: (r) => r.structuralShape === "schema_module" && r.semanticRole === "validation_schema",
  },
  {
    name: "rule:6 — component module with JSX",
    setup: () => ({
      path: "/src/component.tsx",
      content: "import React from 'react'; export const Comp = () => <div>Hello</div>;",
    }),
    invoke: (api, ctx) => api.classifySourceFile(ctx),
    assert: (r) => r.structuralShape === "component_module" && r.semanticRole === "ui_surface",
  },
  {
    name: "rule:7 — CLI module by commander import",
    setup: () => ({
      path: "/src/cli.ts",
      content: "import { Command } from 'commander'; const program = new Command();",
    }),
    invoke: (api, ctx) => api.classifySourceFile(ctx),
    assert: (r) => r.structuralShape === "cli_module" && r.semanticRole === "command_surface",
  },
  {
    name: "rule:9 — executable module with function",
    setup: () => ({
      path: "/src/util.ts",
      content: "export function add(a: number, b: number) { return a + b; }",
    }),
    invoke: (api, ctx) => api.classifySourceFile(ctx),
    assert: (r) => r.structuralShape === "executable_module" && r.semanticRole === "runtime_policy",
  },
];
// Only the cases that self-validated against the source are exported.
export const cases = __generatedCases.filter((_, i) => new Set([0, 1, 2, 4, 5, 6, 7]).has(i));
