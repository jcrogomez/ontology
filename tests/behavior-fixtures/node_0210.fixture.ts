// GENERATED by `onto probe node_0210` (model: qwen3-coder:480b-cloud). Do not hand-edit;
// re-run probe to regenerate. 8 self-validated case(s)
// (each matched the source src/surfaces/walker/state/navigation.ts under the behaviour-checker).
import type { BehaviorCase } from "../../src/laws/behavior-checker.js";
import type { FocalNeighborhood } from "./src/surfaces/walker/state/neighborhood.js";

const __generatedCases: BehaviorCase[] = [
  {
    name: "rule:1 — navigateUp returns parent id when parent exists",
    setup: () => ({
      parent: { id: "parent1", coordinates: { time: 100 } },
      children: [],
      siblings: [],
      focal: { id: "focal1", coordinates: { time: 100 } },
    }),
    invoke: (api, ctx) => (api as any).navigateUp(ctx as FocalNeighborhood),
    assert: (r) => r === "parent1",
  },
  {
    name: "rule:2 — navigateUp returns null when no parent exists",
    setup: () => ({
      parent: null,
      children: [],
      siblings: [],
      focal: { id: "focal1", coordinates: { time: 100 } },
    }),
    invoke: (api, ctx) => (api as any).navigateUp(ctx as FocalNeighborhood),
    assert: (r) => r === null,
  },
  {
    name: "rule:3 — navigateDown returns first child id when children exist",
    setup: () => ({
      parent: null,
      children: [
        { id: "child1", coordinates: { time: 100 } },
        { id: "child2", coordinates: { time: 200 } },
      ],
      siblings: [],
      focal: { id: "focal1", coordinates: { time: 150 } },
    }),
    invoke: (api, ctx) => (api as any).navigateDown(ctx as FocalNeighborhood),
    assert: (r) => r === "child1",
  },
  {
    name: "rule:4 — navigateDown returns null when no children exist",
    setup: () => ({
      parent: null,
      children: [],
      siblings: [],
      focal: { id: "focal1", coordinates: { time: 100 } },
    }),
    invoke: (api, ctx) => (api as any).navigateDown(ctx as FocalNeighborhood),
    assert: (r) => r === null,
  },
  {
    name: "rule:5 — navigateSiblingPrevious returns previous sibling id when exists",
    setup: () => ({
      parent: { id: "parent1", coordinates: { time: 50 } },
      children: [],
      siblings: [
        { id: "sib1", coordinates: { time: 100 } },
        { id: "sib3", coordinates: { time: 100 } },
      ],
      focal: { id: "sib2", coordinates: { time: 100 } },
    }),
    invoke: (api, ctx) => (api as any).navigateSiblingPrevious(ctx as FocalNeighborhood),
    assert: (r) => r === "sib1",
  },
  {
    name: "rule:6 — navigateSiblingPrevious returns null when no previous sibling",
    setup: () => ({
      parent: { id: "parent1", coordinates: { time: 50 } },
      children: [],
      siblings: [
        { id: "sib2", coordinates: { time: 100 } },
        { id: "sib3", coordinates: { time: 100 } },
      ],
      focal: { id: "sib1", coordinates: { time: 100 } },
    }),
    invoke: (api, ctx) => (api as any).navigateSiblingPrevious(ctx as FocalNeighborhood),
    assert: (r) => r === null,
  },
  {
    name: "rule:7 — navigateSiblingNext returns next sibling id when exists",
    setup: () => ({
      parent: { id: "parent1", coordinates: { time: 50 } },
      children: [],
      siblings: [
        { id: "sib1", coordinates: { time: 100 } },
        { id: "sib3", coordinates: { time: 100 } },
      ],
      focal: { id: "sib2", coordinates: { time: 100 } },
    }),
    invoke: (api, ctx) => (api as any).navigateSiblingNext(ctx as FocalNeighborhood),
    assert: (r) => r === "sib3",
  },
  {
    name: "rule:8 — navigateSiblingNext returns null when no next sibling",
    setup: () => ({
      parent: { id: "parent1", coordinates: { time: 50 } },
      children: [],
      siblings: [
        { id: "sib1", coordinates: { time: 100 } },
        { id: "sib2", coordinates: { time: 100 } },
      ],
      focal: { id: "sib3", coordinates: { time: 100 } },
    }),
    invoke: (api, ctx) => (api as any).navigateSiblingNext(ctx as FocalNeighborhood),
    assert: (r) => r === null,
  },
];
// Only the cases that self-validated against the source are exported.
export const cases = __generatedCases.filter((_, i) => new Set([0, 1, 2, 3, 4, 5, 6, 7]).has(i));
