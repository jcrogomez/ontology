// GENERATED by `onto probe node_0139` (model: qwen3-coder:480b-cloud). Do not hand-edit;
// re-run probe to regenerate. 1 self-validated case(s)
// (each matched the source src/inverse/static-summary.ts under the behaviour-checker).
import type { BehaviorCase } from "../../src/laws/behavior-checker.js";
import type { StructuralClassification } from "../src/inverse/structural-classifier.js";

const __generatedCases: BehaviorCase[] = [
  {
    name: "rule:1 — throws on unsupported shape",
    setup: () => ({
      filePath: "/project/src/util.js",
      classification: {
        structuralShape: "unknown_shape",
        language: "javascript",
        vocabulary: undefined,
      } as unknown as StructuralClassification,
    }),
    invoke: (api, ctx) => {
      try {
        return api.buildStaticSummary(ctx);
      } catch (e) {
        return e instanceof Error ? e.message : String(e);
      }
    },
    assert: (r) =>
      typeof r === "string" &&
      r.includes("buildStaticSummary called with unsupported shape") &&
      r.includes("unknown_shape"),
  },
];
// Only the cases that self-validated against the source are exported.
export const cases = __generatedCases.filter((_, i) => new Set([0]).has(i));
